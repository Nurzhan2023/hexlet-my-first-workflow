name: Say Hello Workflow


on: push

jobs:
        build:
                runs-on: ubuntu-latest


                - uses: actions/checkout@v4

                steps:
                        - run: make say-hello
